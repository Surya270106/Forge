FROM python:3.12-slim

RUN useradd -m -u 1000 forge

WORKDIR /app

# Install uv
RUN pip install uv

COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY . .
RUN uv sync --frozen --no-dev

USER forge

CMD ["uv", "run", "python", "-m", "services.worker.main"]
